#!ipxe
###
### netboot.xyz-custom menu example
###

set fedora https://mirror.yandex.ru/fedora/linux/releases/40/Everything/x86_64/os/images/pxeboot

:menu
menu shim transparent loader demo
item fedora        Load Fedora Everything
item exit          Exit
choose option && goto ${option}

:fedora
kernel ${fedora}/vmlinuz initrd=initrd.img repo=${fedora}
initrd ${fedora}/initrd.img
boot

:exit
exit
